# AGENTS.md instructions for google-workspace-gemini

- Slidev로 Google Workspace + Gemini 3일 교육 슬라이드를 만든다.
- 현재 덱은 `curriculm-seah.pdf`를 원천 자료로 삼는다.
- 먼저 Day 1을 완성하되, 3일 전체 흐름 속에서 Day 1의 역할이 보이게 구성한다.
- `lecture-slidev-deck` 스킬의 Paperlogy 폰트, breadcrumb, 아이콘 활용, 검증 규칙을 따른다.
- breadcrumb는 `대주제 > 중주제 > 소주제` 형식으로 쓰고, 맨 앞에 이모지를 붙이지 않는다.
- 본문, 카드, 도식, 산출물 표기에는 이모지와 Iconify 아이콘을 적극 활용한다.
- 검증은 최소 `pnpm run build`, 가능하면 `pnpm run export`까지 수행한다.
